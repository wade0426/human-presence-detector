name: project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pyside6
      - opencv-python
      - ultralytics
      - pyyaml
      - pytest
      - pytest-qt
