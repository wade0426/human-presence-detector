# 人體辨識休息提醒系統 - 依賴套件清單
# 
# 安裝方式: pip install -r requirements.txt
#

# ==================== 核心套件 ====================
# YOLO 物件檢測模型
ultralytics>=8.0.0

# OpenCV 電腦視覺庫
opencv-python>=4.5.0

# 影像處理庫
Pillow>=8.0.0

# 數值計算庫
numpy>=1.21.0

# ==================== GUI 相關 ====================
# Tkinter 通常內建於 Python，但某些環境可能需要額外安裝
# tkinter  # 通常不需要額外安裝

# ==================== 可選套件 ====================
# JSON 處理 (Python 內建，無需安裝)
# json

# 設定檔管理 (Python 內建，無需安裝)  
# configparser

# 日期時間處理 (Python 內建，無需安裝)
# datetime

# 執行緒處理 (Python 內建，無需安裝)
# threading

# ==================== 系統需求說明 ====================
# Python 版本: 3.8+
# 作業系統: Windows 10/11 (主要測試環境)
# 硬體需求: 
#   - 可用的攝像頭設備
#   - 至少 4GB RAM (推薦 8GB+)
#   - 支援 CUDA 的顯示卡 (可選，用於加速 YOLO 推理)

# ==================== 安裝注意事項 ====================
# 1. 確保 Python 版本為 3.8 或以上
# 2. 建議在虛擬環境中安裝依賴
# 3. 如果有 CUDA 顯示卡，可安裝 torch 的 CUDA 版本以提升效能
# 4. 首次執行時，YOLO 模型會自動下載 (約 6MB)

# ==================== 常見問題解決 ====================
# 如果 opencv-python 安裝失敗，可嘗試:
# pip install opencv-python-headless

# 如果在某些 Linux 系統上 tkinter 不可用，可安裝:
# sudo apt-get install python3-tk

# 如果需要 GPU 加速，可額外安裝:
# torch>=1.9.0
# torchvision>=0.10.0

# ==================== 開發環境套件 (可選) ====================
# 以下套件僅在開發時需要，使用者執行程式時不需要

# 程式碼格式化
# black>=22.0.0

# 程式碼檢查
# flake8>=4.0.0

# 型別檢查
# mypy>=0.910

# 測試框架
# pytest>=6.0.0

# 文檔生成
# sphinx>=4.0.0
